@using Labels = SAS.Web.Resources.Labels;

@Html.DevExpress().Memo(
    settings => {
        MemoFunctions.ApplyDefault(settings, "memoAdditionallInformation", Labels.AdditionalInformation);
    }).GetHtml()
