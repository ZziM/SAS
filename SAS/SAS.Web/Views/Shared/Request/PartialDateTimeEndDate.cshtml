@using Labels = SAS.Web.Resources.Labels;

@Html.DevExpress().DateEdit(settings => {

    DateEditFunctions.ApplyDefault(settings, "dateTimeEndAccessDate", Labels.EndAccessDate);

    settings.Properties.MinDate = DateTime.Now;
}).GetHtml()