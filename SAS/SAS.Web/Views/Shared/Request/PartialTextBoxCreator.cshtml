@using Labels = SAS.Web.Resources.Labels;

@Html.DevExpress().TextBox(settings =>
{
    TextBoxFunctions.ApplyDefault(settings, "textBoxCreator", Labels.Creator);
    
    settings.ReadOnly = true;
}).Bind(@"JTICORP\CSTMAKILOD").GetHtml()